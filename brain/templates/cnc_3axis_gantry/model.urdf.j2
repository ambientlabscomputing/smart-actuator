<?xml version="1.0"?>
<!--
  cnc_3axis_gantry — URDF placeholder.

  This file is NOT used at runtime.  The Brain generates the URDF
  generically from the dh: block in template.yaml via dh_urdf.py.
  A model.urdf.j2 is only needed for legacy Jinja-expanded templates.

  If you need a hand-authored URDF for this template, replace this file
  with a valid Jinja2 URDF template (see two_dof_planar_arm/model.urdf.j2
  as an example) and set `use_generic_dh: false` in template.yaml.
-->
<robot name="cnc_3axis_gantry"/>
