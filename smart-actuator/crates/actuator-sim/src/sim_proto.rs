/// Includes the tonic-generated code for `actuator_sim.proto`.
/// This module is intentionally scoped to `actuator-sim` — it must never be
/// pulled into `actuator-core` or `actuator-firmware`.
pub mod actuator_sim {
    tonic::include_proto!("actuator_sim");
}
